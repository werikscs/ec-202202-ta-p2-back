const enum CustomErrorMessages {
  USER_NOT_REGISTERED = 'User not registered',
  INCORRECT_CREDENTIALS = 'Incorrect Credentials',
  EMAIL_ALREADY_TAKEN = 'Email already taken',
}

export default CustomErrorMessages
